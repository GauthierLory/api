{% extends 'base.html.twig' %}

{% block title %}Edit Article{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header">
            {{ form_start(formP) }}
            {{ form_widget(formP) }}
            {{ form_end(formP) }}
        </div>
    </div>
{% endblock %}
