<nav class="navbar navbar-expand-md navbar-dark bg-primary fixed">
    <input type="hidden" id="language_sess" value = "{{ app.session.get('language') }}">
    <a class="navbar-brand" href="{{ path('home') }}">{{ 'nav.home'|trans }}</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
        <ul class="navbar-nav mr-auto">
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    <img id="nav_lang" width="25px" height="25px" src="{{ asset('./images/uploads/icone-kit.jpg') }}">
                </a>
                <div id="flag_dropdown" class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <a id="en" class="dropdown-item" href="{{ path('switch_language_english') }}">
                        <img id="flag_en" width="25px" height="25px" src="{{ asset('./images/uploads/icone-en.gif') }}">
                    </a>
                    <div class="dropdown-divider"></div>
                    <a id="fr" class="dropdown-item" href="{{ path('switch_language_french') }}">
                        <img id="flag_fr"  width="25px" height="25px" src="{{ asset('./images/uploads/icone-fr.jpg') }}">
                    </a>
                </div>
            </li>
        </ul>
    {% endif %}
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ml-auto">
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <li><a class="btn btn-primary" data-toggle="modal" data-target="#cart"><i class="fa fa-shopping-cart"></i>(<span class="total-count"></span>)</a></li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        {{ app.user.username }}
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="{{ path('profile_page') }}">{{ 'nav.profile'|trans }}</a>
                        <a class="dropdown-item" href="{{ path('app_logout') }}">{{ 'app.logout'|trans }}</a>
                    </div>
                </li>
                <li class="nav-item">
                    <img src="{{ asset('./images/uploads/' ~ app.user.avatar) }}" height="40px" width="40px">
                </li>
            {% else %}
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_login') }}">{{ 'app.login'|trans }}</a>
            </li>

            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_register') }}">{{ 'app.register'|trans }}</a>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    <i class="fas fa-cog"></i>
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="{{ path('switch_language_english') }}">EN</a>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="{{ path('switch_language_french') }}">FR</a>
                </div>
            </li>
            {% endif %}
        </ul>
    </div>
</nav>