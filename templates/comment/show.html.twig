{% extends 'base.html.twig' %}

{% block title %}comment{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header"><h1><a class="btn btn-primary" href="{{ path('comment_index') }}"><i class="fas fa-arrow-alt-circle-left"></i></a> comment</h1></div>

        <div class="card-body">
            <table class="table">
                <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ comment.id }}</td>
                </tr>
                <tr>
                    <th>Description</th>
                    <td>{{ comment.description }}</td>
                </tr>
                <tr>
                    <th>Rating</th>
                    <td>{{ comment.rating }}</td>
                </tr>
                </tbody>
            </table>
            <a class="btn btn-warning btn-sm" href="{{ path('comment_edit', {'id': comment.id}) }}">edit</a>

            {{ include('comment/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
