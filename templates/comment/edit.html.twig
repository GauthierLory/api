{% extends 'base.html.twig' %}

{% block title %}Edit Comment{% endblock %}
{% block body %}
    <div class="card">
        <div class="card-header"><h1><a class="btn btn-primary" href="{{ path('comment_index') }}"><i class="fas fa-arrow-alt-circle-left"></i></a> Edit Comment</h1></div>

            {{ include('comment/_form.html.twig') }}
        </div>
        {{ include('comment/_delete_form.html.twig') }}

    </div>
{% endblock %}
