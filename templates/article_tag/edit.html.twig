{% extends 'base.html.twig' %}

{% block title %}Edit ArticleTag{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header"><h1><a href="{{ path('article_tag_index') }}"><i class="fas fa-arrow-alt-circle-left"></i></a> Edit ArticleTag</h1></div>
        <div class="card-body">
            {{ include('article_tag/_form.html.twig', {'button_label': 'Update'}) }}

            {{ include('article_tag/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
