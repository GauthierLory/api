{% extends 'base.html.twig' %}

{% block title %}ArticleTag{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header"><h1><a href="{{ path('article_tag_index') }}"><i class="fas fa-arrow-alt-circle-left"></i></a> ArticleTag</h1></div>
        <div class="card-body">
            <table class="table">
                <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ article_tag.id }}</td>
                </tr>
                <tr>
                    <th>Name</th>
                    <td>{{ article_tag.name }}</td>
                </tr>
                <tr>
                    <th>Description</th>
                    <td>{{ article_tag.description }}</td>
                </tr>
                </tbody>
            </table>
            <a class="btn btn-warning btn-sm" href="{{ path('article_tag_edit', {'id': article_tag.id}) }}">edit</a>

            {{ include('article_tag/_delete_form.html.twig') }}
        </div>
    </div>



{% endblock %}
