{% extends 'base.html.twig' %}

{% block title %}ProductTag index{% endblock %}

{% block body %}
    <h1>ProductTag index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for product_tag in product_tags %}
            <tr>
                <td>{{ product_tag.id }}</td>
                <td>{{ product_tag.name }}</td>
                <td>{{ product_tag.description }}</td>
                <td>
                    <a href="{{ path('product_tag_show', {'id': product_tag.id}) }}">show</a>
                    <a href="{{ path('product_tag_edit', {'id': product_tag.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('product_tag_new') }}">Create new</a>
{% endblock %}
