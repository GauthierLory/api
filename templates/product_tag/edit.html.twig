{% extends 'base.html.twig' %}

{% block title %}Edit ProductTag{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header"><h1>Edit ProductTag</h1></div>
        <div class="card-body">
            {{ include('product_tag/_form.html.twig', {'button_label': 'Update'}) }}

            <a href="{{ path('product_tag_index') }}">back to list</a>

            {{ include('product_tag/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
